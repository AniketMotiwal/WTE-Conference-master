/** @type {import('tailwindcss').Config} */
export default {
  content: [
    "./index.html",
    "./src/**/*.{js,ts,jsx,tsx}",
  ],
  theme: {
    extend: {
      keyframes: {
        fadeIn: {
          '0%': { opacity: '0' },
          '100%': { opacity: '1' },
        },
        slideIn: {
          '0%': { transform: 'translateY(20px)', opacity: '0' },
          '100%': { transform: 'translateY(0)', opacity: '1' },
        },
      },
      animation: {
        fadeIn: 'fadeIn 3s ease-out infinite',   // Infinite loop for fadeIn with 3s duration
        slideIn: 'slideIn 3s ease-out infinite', // Infinite loop for slideIn with 3s duration
      },
      
    },
  },
  plugins: [],
};
